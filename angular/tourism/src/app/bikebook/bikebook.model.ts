export class BikeBookModel {

    constructor(public name: string, public company: string, public color: string, public model: string, public engineCC: number) {
        console.log('created BikeBookModel');

    }
}